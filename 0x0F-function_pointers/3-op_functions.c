#include "3-calc.h"

/**
 * op_add - adds two
 * @a: first
 * @b: seconed
 * Return: sum
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - subtracts two
 * @a : first
 * @b: seconed
 * Return: difference
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multipluy  two
 * @a : first
 * @b: seconed
 * Return: difference
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div  - divid  two
 * @a : first
 * @b: seconed
 * Return: difference
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod  - moudle  two
 * @a : first
 * @b: seconed
 * Return: difference
 */

int op_mod(int a, int b)
{
	return (a % b);
}

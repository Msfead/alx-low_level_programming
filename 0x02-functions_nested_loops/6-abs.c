#include "main.h"
/**
*main - Entry point
*Descraption:computes the absolute value of an integer.
*Return: Always 0
*/
int _abs(int)

	int i; 
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
